% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_basemap_raw.R
\name{render_basemap_raw}
\alias{render_basemap_raw}
\title{Render a basemap and return raw RGBA bytes.}
\usage{
render_basemap_raw(
  bbox_3857,
  width,
  height,
  style_input,
  zoom = NULL,
  tile_timeout_ms = 10000L,
  max_tiles = 256L,
  layers = NULL
)
}
\arguments{
\item{bbox_3857}{Numeric vector of length 4: \code{c(xmin, ymin, xmax, ymax)}
in EPSG:3857 metres.}

\item{width}{Integer output pixel width.}

\item{height}{Integer output pixel height.}

\item{style_input}{Character: a MapLibre GL style URL or inline JSON string.}

\item{zoom}{Integer zoom level (0–22), or \code{NULL} for automatic selection.}

\item{tile_timeout_ms}{Integer per-tile HTTP timeout in milliseconds.}

\item{max_tiles}{Integer maximum tile downloads per render.}

\item{layers}{Character vector of layer IDs to filter. Plain IDs keep only
those layers; minus-prefixed IDs (e.g. \code{"-roads"}) exclude those layers.
Mixed positive/negative lists raise an error. \code{NULL} renders all layers.}
}
\value{
A \code{raw} vector of length \code{width * height * 4} (RGBA, row-major).
}
\description{
Low-level wrapper around the Rust FFI. The returned raw vector must be
reshaped before use; see the example below.
}
\examples{
\dontrun{
raw <- render_basemap_raw(
  bbox_3857   = c(-13700000, 4500000, -13600000, 4600000),
  width       = 400L,
  height      = 300L,
  style_input = "https://demotiles.maplibre.org/style.json"
)
m <- array(as.integer(raw), dim = c(4L, 400L, 300L))
m <- aperm(m, c(3, 2, 1))  # [height, width, channels]
}
}
